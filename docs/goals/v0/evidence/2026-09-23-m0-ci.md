# M0 CI 证据（2026-09-23）

仓库：`char-pub/char`（public）。本地 main 与远端一致，最新提交 `78b1824`。

## M0-1：monorepo 骨架与 `pnpm ci:all`

最近四次推送的 CI 全部成功：

| commit | ci（check + dev-env） | codeql | scorecard |
|---|---|---|---|
| `c601fa2` | run 35822700874 success | success | success |
| `f71047c` | run 35825309389 success | success | success |
| `17e1ad9` | run 35827535151 success | success | success |
| `78b1824` | run 35828115487 success | success | success |

`check` job 在 GitHub runner 上执行 `pnpm ci:all`：lint、typecheck、依赖边界、单元测试（带覆盖率门槛）、web 与 admin 组件测试、三个运行时的一致性测试、集成测试（Testcontainers，带覆盖率门槛）、build、web 与 admin 的 Playwright、Action 产物检查、gitleaks。结论：**M0-1 可以勾选**。

## M0-4：干净机器上的本地开发步骤

同一批 run 中的 `dev-env` job 在全新的 runner 上执行 README 的步骤：`pnpm install --frozen-lockfile` → `pnpm infra:up` → `pnpm dev:smoke`（启动 api、worker、web，检查 web、经代理的 API、匿名 `/v1/me` 401、worker 健康检查、`dev:login` 会话有效、Ctrl-C 以 0 退出）。四次全部成功（最近一次 run 35828115487，步骤 `pnpm infra:up` 与 `pnpm dev:smoke` 均为 success）。本机的全新 clone 走查见 [2026-09-22-local-dev-walkthrough.md](2026-09-22-local-dev-walkthrough.md)。结论：**M0-4 可以勾选**。

## M0-2：公开仓库安全基线

| 要求 | 证据 | 结论 |
|---|---|---|
| `LICENSE`（Apache-2.0）、`spec/LICENSE`（CC-BY-4.0）、`SECURITY.md`、`CODEOWNERS`、Renovate 配置 | 文件都在仓库中（`.github/CODEOWNERS`、`renovate.json`） | 满足 |
| 所有 workflow 声明 `permissions: contents: read`，只在 job 上按需加权限 | ci、codeql、scorecard、dependency-review 顶层都是 `contents: read` | 满足 |
| 第三方 Action 固定到 SHA | 四个 workflow 中所有 `uses:` 都是 40 位 SHA | 满足 |
| gitleaks（CI + pre-commit） | CI 的 `ci:all` 最后一步执行 `pnpm secrets`；lefthook pre-commit 执行 gitleaks | 满足 |
| CodeQL | 四次推送均 success | 满足 |
| 依赖审查 | 用一个只改 README 空行的 draft PR（#1，已关闭、分支已删除）触发，失败：`Dependency review is not supported on this repository. Please ensure that Dependency graph is enabled`。仓库的 dependency graph 未开启 | **缺口** |
| Renovate 实际运行 | 仓库里有配置，但组织没有安装 Renovate GitHub App | **缺口** |

结论：M0-2 暂不勾选。需要用户在仓库设置中开启 Dependency graph（Settings → Code security），并为 `char-pub` 组织安装 Renovate App；两者都属于修改组织或仓库设置，需要用户操作或单独授权。开启后重新开一个探测 PR 验证依赖审查。

同时注意到仓库的 secret scanning 与 push protection 为 disabled，这属于 M0-3，同样需要用户授权修改组织设置。
