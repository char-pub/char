---
description: "GitHub 关联身份、App 安装、仓库写权限 lookup/bind/rebind、转移冻结、webhook、Action OIDC 发布与 GITHUB_OUTPUT 文件协议"
kind: architecture
code:
  paths:
    - "apps/server/src/api/routes/github-bindings.ts"
    - "apps/server/src/github/source.ts"
    - "apps/web/src/components/source-connect.tsx"
    - "apps/web/src/components/source-binding.tsx"
    - "actions/publish/action.yml"
    - "actions/publish/src/run.ts"
    - "actions/publish/src/output.ts"
    - "apps/server/src/github/bindings.ts"
    - "apps/server/src/github/events.ts"
    - "apps/server/src/github/load-source.ts"
    - "apps/server/src/webhook/github.ts"
    - "apps/server/src/worker/github.ts"
    - "apps/server/src/oidc/github.ts"
    - "apps/server/src/api/routes/github-webhook.ts"
    - "apps/server/src/api/routes/oidc-publish.ts"
relations:
  requires:
    - "registry/publishing.mdx"
    - "authoring/cli-workflow.mdx"
  related:
    - "identity/creator-and-guest.mdx"
    - "identity/public-authorization.mdx"
---

# GitHub 来源与发布信任链

GitHub App 绑定使用不可复用的数字仓库及 owner 身份，不能只靠可改名的仓库路径。转移或所有者不一致时冻结来源，待重新确认后再恢复。webhook 验证原始请求字节的 HMAC，按 delivery 去重；push 同步来源状态，不自动发布 Release。

浏览器连接先关联 GitHub 身份、安装 App，再查找仓库。App 能访问仓库只证明安装授权，不能证明当前创作者有绑定权；lookup 在返回私有仓库信息前，bind 和冻结后的 rebind 在写入前，都要用已关联的 GitHub 数字账户身份重新验证仓库写权限。数字身份解析为当前路径或 login 后，还须核对权限响应里的数字身份，避免改名竞争把权限授予另一账户。

Publish Action 在允许的 push、workflow_dispatch 或 release 工作流中计算本地摘要并申请 OIDC token；PR 事件不得取得发布路径。OIDC 证明的是受约束的仓库工作流身份、ref、sha、事件和 token 新鲜度，不证明运行的 Action 源码一定可信。Registry 校验这些声明和重放标识后，用 GitHub App 权限在该 commit 重新读取源文件，按同一项目解析算法重算 digest。Action 提交的摘要只是比对值，不是最终内容权威。

来源验证成功后进入普通 Revision 与异步发布流程，仍接受 Registry 的权限、依赖、资产和黑名单检查。修改 Action 与服务端任一侧算法时需共同验证 digest 一致和失败重试。

Action 与 Server 源重读都经统一 artifact 构建，比较 artifact.root.semantic_digest 而非产物 digest。技术作者提供明确的依赖快照，Action dry-run 也执行确定性作者测试；此成功只证明受给定输入约束的本地结果，Server 仍从可信 commit 重读并在普通发布流程中检查当前依赖授权、许可及状态；重读作品类型必须与已绑定的 Registry Creation 一致，不能借更新源文件静默更换身份类型。

Action 输出使用 runner 提供的既有 `GITHUB_OUTPUT` 普通文件：打开后在同一文件描述符上校验并追加，拒绝目标文件符号链接，避免先检查路径再重新打开的竞争。输出名与多行分隔符须验证，值保持字面数据；缺少合法输出文件时失败，不退回把值作为工作流命令输出。
